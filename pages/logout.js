import React from "react";
import Head from "next/head";

const Logout = () => (
  <div>
    <Head>
      <title>Logout</title>
    </Head>
    You are being logged out...
  </div>
);

export default Logout;
